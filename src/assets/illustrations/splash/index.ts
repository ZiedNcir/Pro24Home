export const SplashIllustrations = {
  OrangeBackground: require('./final/splash_orange_background.png'),
};

// Reference only. Do not use this image in production screens.
// It may contain generated UI elements that must be rebuilt in code.
export const SplashReferences = {
  ValidatedPrototype: require('./reference/splash_validated_prototype_reference.png'),
};
